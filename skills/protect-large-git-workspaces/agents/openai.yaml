interface:
  display_name: "Protect Large Git Workspaces"
  short_description: "Guard Git from large or many files"
  default_prompt: "Use $protect-large-git-workspaces to scan this workspace for large-file Git risk before any broad Git write, snapshot, or cleanup operation."

policy:
  allow_implicit_invocation: true
